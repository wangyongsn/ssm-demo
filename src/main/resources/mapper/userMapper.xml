<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xwtec.dao.UserMapper">
    <select id="qryUser" resultType="java.util.Map">
      SELECT * FROM BB_MAP_T_USER WHERE F_LOGIN_NAME='wangyong'
    </select>
</mapper>